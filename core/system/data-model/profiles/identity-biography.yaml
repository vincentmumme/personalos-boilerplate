name: identity-biography
page_shape: state-record
canonical_owner: identity-biography
allowed_roots: [identity]
path_pattern: ^identity/biography\.md$
required_modules: [lifecycle, subject-context]
optional_modules: []
required_fields: [lifecycle, subject_ref]
optional_fields: []
fields: {}
module_constraints:
  lifecycle:
    enum: [active, retired]
required_sections: [Current Truth, Life Phases, Formative Events, Biography Gaps, Sources, Timeline]
section_order: [Current Truth, Life Phases, Formative Events, Biography Gaps, Sources, Timeline]
template: system/templates/identity/biography.md
admission:
  positive_fixture: system/data-model/tests/fixtures/valid-identity-biography.fixture
  positive_logical_path: identity/biography.md
  negative_fixture: system/data-model/tests/fixtures/invalid-identity-biography.fixture
  negative_logical_path: identity/biography.md
  validator_test: system/data-model/tests/test_registry_contract.py
  migration_mapping: system/data-model/legacy-mapping.md
