name: identity-capabilities
page_shape: state-record
canonical_owner: identity-capabilities
allowed_roots: [identity]
path_pattern: ^identity/capabilities\.md$
required_modules: [lifecycle, subject-context]
optional_modules: []
required_fields: [lifecycle, subject_ref]
optional_fields: []
fields: {}
module_constraints:
  lifecycle:
    enum: [active, retired]
required_sections: [Current Truth, Capability Areas, Demonstrated Experience, Evidence and Credentials, Limits, Related Development Projects, Sources, Timeline]
section_order: [Current Truth, Capability Areas, Demonstrated Experience, Evidence and Credentials, Limits, Related Development Projects, Sources, Timeline]
template: system/templates/identity/capabilities.md
admission:
  positive_fixture: system/data-model/tests/fixtures/valid-identity-capabilities.fixture
  positive_logical_path: identity/capabilities.md
  negative_fixture: system/data-model/tests/fixtures/invalid-identity-capabilities.fixture
  negative_logical_path: identity/capabilities.md
  validator_test: system/data-model/tests/test_registry_contract.py
  migration_mapping: system/data-model/legacy-mapping.md
